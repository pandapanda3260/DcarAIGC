# 步骤 10：切换、全量回归与真实端到端验收

日期：2026-08-02（Asia/Shanghai）

## 本步修改

- 删除不再被路由引用的旧前端组件 `app/web/app/EvaluationDashboard.tsx`，并清理 `app/web/public/data`、`app/web/public/exports` 的旧静态 v7 壳；v7 的 5 个历史 revision 继续通过只读 API 提供。
- 将无法由补丁工具读取的旧 PNG 移至 macOS 废纸篓：`/Users/mark/.Trash/DcarAIGC-v8-cleanup-20260802/双渠道核心结论_v6_TikHub补充_2026-08-02.png`，可人工恢复。
- 更新根 README、Web README、启动脚本和运行/备份手册；启动脚本固定绑定 `127.0.0.1:4173`、`127.0.0.1:8765`，端口占用时立即失败，不再静默漂移到 4174。
- 页面状态栏显示真实 schema `SQLite v3`；日期窗口明确标注“结束（不含）”；空账号页提示改为当前可执行操作。
- 对全仓 Python 做不改变行为的 Ruff 自动修复，并手工消除 5 项剩余告警；未删除测试、未弱化断言、未增加忽略规则。

## 验证结果

### 依赖与静态检查

- `python3 -m uv sync --frozen --all-groups`：通过，锁文件内 49 个包完成一致性检查。
- `.venv/bin/ruff check src/dcar_eval tests`：通过。
- `.venv/bin/mypy src/dcar_eval/v8`：通过，14 个 v8 生产模块无错误。
- `npm run lint`：通过。
- `npm exec --yes --package=node@22.13.1 -- tsc --noEmit`：通过。

全仓旧 v3–v7 脚本的探索性 `mypy` 检查仍报告 88 项历史类型债务，主要为旧式顶层导入、第三方 stub 缺失和旧 `Any` 数据结构；本次没有用 `ignore` 配置制造假通过。v8 当前生产边界与全部 Web 代码均已通过类型门禁。

### 测试与构建

- `PYTHONPATH=src/dcar_eval .venv/bin/python -m unittest discover -s tests -v`：234/234 通过，耗时 8.764 秒。
- `npm test`：vinext 五阶段生产构建通过；3/3 渲染测试通过。
- Web 构建仅输出 Node `punycode` 弃用提示、实验性 glob 提示和 vinext 路由静态分类提示，无构建失败。

首次直接运行 `unittest discover` 时漏传旧仓库要求的 `PYTHONPATH=src/dcar_eval`，29 个模块因此无法导入；按仓库真实启动约定修正命令后重新执行，234 项全部通过。此失败没有被计为通过。

### 数据库与历史合同

- `PRAGMA integrity_check`：`ok`。
- `PRAGMA foreign_key_check`：0 行。
- v8 内容：776 条，其中抖音 438、小红书 338。
- 复核队列：manual_required 164、pending 9、resolved 1、terminal_failed 12。
- 自动报告：日报 1、周报 1，均为 succeeded；10 个 run-scoped 报告文件已登记。
- `/api/v7/history/reports`：保留 5 个 `channel-structured-conclusions-v7.0` 历史 revision，只读可访问。

## 真实端到端实跑

在真实本地数据库、真实 FastAPI 服务和真实 vinext 页面完成以下链路：

1. 概览页读取 API、三种上海时区窗口、`SQLite v3`、右开日期边界和数据质量状态。
2. 账号页新增测试账号、以完整手机号 POST 搜索、修改运营人员并验证页面回显。
3. 内容页新增视频号内容、关联测试账号、按标题和平台组合筛选、修改标题并验证页面回显。
4. 任务页打开真实周报 `D8-W-20260720-20260726`，逐一验证报告概览、平台维度、账号/方向、内容明细、文件与日志 5 个页签；确认 content CSV、JSON、Markdown、PNG、SVG 共 5 个下载路由，JSON 下载为 HTTP 200、124,976 字节。
5. 卖点页进入 `selling-points-v5.1` 草稿编辑态并打开 C1 编辑器，不发布草稿。
6. 浏览器日志无 error；清洁重启后 API 健康检查返回 `mode=local_v8`，Web 首页返回 HTTP 200；4173 与 8765 均只有一个监听进程。
7. 精确删除本次 E2E 产生的 1 个测试内容、1 个测试账号和 1 个未发布草稿；清理后内容恢复 776 条，发布词表仍为 `selling-points-v5.0`。

此前真实供应商链路另完成 1 条抖音统计更新：内容 `ADWCP9` 的 TikHub 指标槽成功，写入 SHA-256 原始响应和新评估版本；阅读/播放量 401,680、点赞 7,768、分享 48。

## 费用

- 数据库可审计费用：TikHub 1 次成功计费请求，USD 0.001；Rnote 20 次 HTTP 402 小样本请求，成功计费 0 次，USD 0。
- 为核实三个 TikHub 端点单价调用过其计价查询接口；这些查询没有进入产品 usage 表。按接口返回的单次 USD 0.001 计，额外上限 USD 0.003。
- 本轮外部费用合计：可审计产品调用 USD 0.001；含计价查询的保守上限 USD 0.004。

## 本步发现的问题与修复

- 旧 4173 进程导致新实例静默启动到 4174：终止当前仓库的陈旧进程，并在启动脚本增加端口前置检查和显式绑定；二次启动验证会以退出码 1 和明确占用提示失败。
- 页面显示 `SQLite v2`：改为当前 schema v3，并回归页面。
- 概览日期的结束值容易被理解为闭区间：改为“结束（不含）”。
- 空账号页仍写“写入功能将在后续启用”：改为“新增账号/批量导入”的当前操作提示。
- HMR 期间出现一次 React 多 renderer 警告：停止热更新进程并清洁重启；新进程日志不再出现该警告。

## 遗留风险

- 165 条小红书媒体补证已完成路由设计，但 Rnote 当前返回 HTTP 402，尚不能放量；20 条试采均未计费，剩余记录保持可审计的人工/终态路线。
- 164 条 `manual_required` 与 9 条 `pending` 仍需运营人员在内容页逐条复核；12 条已明确终态不可用。
- 预估拉新、拉活、线索首版没有经验证模型，合同和页面继续返回 `not_calculable`，不会伪造百分比。
- v3–v7 冻结历史脚本尚未全仓类型清零；它们的 165 项旧断言已由当前 234 项全集覆盖，v8 生产模块类型门禁通过。
- vinext 仍输出上游 `punycode` 弃用、实验性 glob 和动态路由分类提示；不影响当前构建或运行，但升级 vinext/Vite 时需要复查。
