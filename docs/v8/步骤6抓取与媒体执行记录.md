# 步骤 6：抓取与媒体执行记录

执行日期：2026-08-02（Asia/Shanghai）

## 已落地边界

- 供应商调用统一经过 `fetch_slots`、`fetch_attempts`、`provider_raw_responses`、`provider_usage` 和 `provider_budget_batches`；成功槽不重复请求，失败槽保留显式重试状态。
- 原始响应在写库前落为权限 `0600` 的 JSON，登记相对路径、字节数和真实 SHA-256；鉴权字段递归替换为 `[REDACTED]`。
- Rnote 历史媒体补证预算固定为 0.008 USD/成功请求、试点 20 次/0.16 USD、总计 164 次/1.312 USD、每日 20 次。价格于 2026-08-02 从 `https://rnote.dev/pricing` 复核；失败请求不计费。
- ASR 固定为 `mlx-whisper==0.4.3`、`mlx==0.32.0`、模型 `mlx-community/whisper-large-v3-turbo` revision `a4aaeec0636e6fef84abdcbe3544cb2bf7e9f6fb`。
- OCR 统一使用 `runtime/bin/vision_ocr`；抖音和小红书视频均执行关键帧、联系表、ASR、OCR，不再从缓存目录或 `runtime/dependencies/video` 注入依赖。

## 真实验证

- 抖音内容 `content_id=100` 完整执行一次本地媒体管线，`frames/asr/ocr` 三个槽均 `succeeded`、`attempt_count=1`；重复执行复用成功槽。
- 165 条历史 V1 媒体路由：1 条本地媒体修正旧 OCR 聚合后为 `resolved`；164 条建立 `media_source_refresh/lifetime` 唯一槽。
- Rnote 试点请求返回 20 次 HTTP 402。供应商未扣费，数据库 `consumed_requests=0`、`consumed_amount=0.0`。
- 首次实现把 HTTP 402 错误归入内容终态且未首条熔断。该 BUG 已修复：401/402/403 现在进入 provider-blocked 熔断，抓取槽为 `retryable_failed`，不进入内容质量门槛。受本次供应商余额状态影响，164 条全部进入 `manual_required`，1 条为 `resolved`；这表示全部到达明确路由，不表示补成 V2/V3。

## 可恢复清理

- 旧 `runtime/dependencies/video`（实测 1,171,228 KiB，约 1.2 GB / 1.1 GiB）已移动到 `/Users/mark/.Trash/DcarAIGC-video-dependencies-20260802`，可从废纸篓恢复。
- venv 中的 MLX 依赖与固定模型缓存均在清理后再次验证可加载。
