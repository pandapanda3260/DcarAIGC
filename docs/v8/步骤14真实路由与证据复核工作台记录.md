# 步骤 14：真实路由与证据复核工作台记录

日期：2026-08-02

## 修改内容

- 将 1256 行单体 `InsightDashboard.tsx` 拆为 `/overview`、`/tasks`、`/tasks/[id]`、`/accounts`、`/contents`、`/selling-points` 六组真实路由，并抽取共享外壳、反馈组件、API、类型和格式化模块。
- 内容证据弹窗接入本地媒体、ASR 全文、OCR 全文、评论摘要和媒体处理槽；人工复核提交携带 `base_evaluation_id`，遇到过期评估返回 409 后刷新证据，不静默覆盖。
- 媒体重试明确区分零成本本地重试和需二次确认的付费详情刷新；后端补齐证据、媒体处理搜索和重试 API。
- 任务补齐取消与恢复 API，并在执行安全点落地状态转换；任务详情保留重试、取消、恢复和按任务文件下载入口。
- 账号导入按平台 UID 自动回填内容归属；新增 `pending_platform_identities`，不伪造手机号地登记 30 个抖音待归属身份。
- 数据库 schema 升至 v6；升级前备份为 `app/data/backups/dcar_insight-before-v6-routes-20260802-235030.sqlite3`。

## 验证命令与结果

- `PYTHONPATH=src/dcar_eval .venv/bin/pytest -q tests/test_v8_storage.py tests/test_v8_operations.py tests/test_v8_providers.py tests/test_v8_reports.py tests/test_v8_api.py`：49 项通过。
- `.venv/bin/ruff check src tests`：通过。
- `PYTHONPATH=src/dcar_eval .venv/bin/mypy src/dcar_eval/v8`：14 个 v8 源文件通过。
- `npm run lint`：通过。
- `npm test`：Next.js 生产构建通过，7 个路由生成成功，4 项前端合同测试通过。
- `npm exec --yes --package=node@22.13.1 -- node ./node_modules/typescript/bin/tsc --noEmit`：通过。
- SQLite `PRAGMA integrity_check`：`ok`；`PRAGMA foreign_key_check`：0 条异常；待归属身份 30 条，均为抖音 UID。
- 真实浏览器：概览、账号、内容、任务列表、任务详情、卖点六页均可访问且控制台 0 error；内容证据弹窗实测显示 1 个本地媒体、170 字 ASR、99 字 OCR，以及评论摘要和处理槽区域。

## 发现的问题及修复情况

- 前端合同测试最初发现任务操作路径由字符串拼接隐式生成，无法证明 `/retry`、`/cancel`、`/resume` 三条真实合同均存在；已改为显式 `taskActionPaths` 映射并重跑通过。
- 全仓 `.venv/bin/mypy src` 仍报告 132 条旧 v2-v7 脚本历史错误，涉及裸导入和未纳入运行环境的 Pillow 等依赖。本步骤未降低断言或掩盖结果；生产 v8 包的严格类型检查全部通过，旧脚本错误留作独立治理范围。

## 真实运行与费用

- 使用真实产品库完成 schema v6 迁移和页面/API 读取验收，没有触发供应商调用。
- 本步骤新增费用：USD 0。

## 下一步

实现感知重复识别：生产媒体字节 SHA-256、抽帧 pHash、正文/ASR/OCR SimHash，建立 150 对人工标注定标集和 95% 精确率发布闸门，将确认关系接入自动媒体链路、内容页和报告口径。
