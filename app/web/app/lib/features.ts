// Publication dates are part of the normal content-search contract. Keep reads
// on the authenticated API so the installed reader and its cache stay in use.
export const CONTENT_SEARCH_PATH = "/api/v8/contents/search";
