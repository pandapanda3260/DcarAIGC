# DCar Insight Web

本地 v8 内容运营工作台，通过 `http://127.0.0.1:8765/api/v8` 读取 SQLite 当前状态。页面不再携带 v7 静态报告副本；v7 历史通过后端只读历史路由访问。

当前版本开放概览、不可变报告任务、账号和内容 CRUD/CSV、逐条数据更新、人工复核、卖点标准版本化编辑与报告下载。付费抓取受成功槽、预算和熔断保护。

从项目根目录运行 `scripts/start_web_mvp.sh` 可同时启动网页和任务服务。
