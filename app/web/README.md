# DCar Insight Web

本地 v8 内容运营工作台通过认证网关 `http://127.0.0.1:4173` 访问 8766 正式 API 和 SQLite 当前状态。页面不再携带 v7 静态报告副本；v7 历史通过后端只读历史路由访问。

当前版本开放概览、不可变报告任务、账号和内容 CRUD/CSV、逐条数据更新、人工复核、卖点标准版本化编辑与报告下载。付费抓取受成功槽、预算和熔断保护。

从项目根目录运行 `scripts/start_web_mvp.sh` 会先验证既有 8766 writer，再启动 4174 Web 和 4173 认证网关；它不会启动或重启 API。页面上的显式写操作直接作用于正式库，8765 只用于 operator freeze 下的只读快照。
